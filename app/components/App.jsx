var React = require('react');

var App = React.createClass({
    render: function () {
        return (
            <div>
                App.jsx
            </div>
        )
    }
});

module.exports = App;